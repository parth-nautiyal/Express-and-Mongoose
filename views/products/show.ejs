<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>Price = <%= product.price %></ul>
    <ul>Category = <a href="/products?category=<%= product.category %>"><%= product.category %></a></ul>
    <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>
    <a href="/products">Go back to all products</a>
    <a href="/products/<%= product.id %>/edit">Edit Product</a>
</body>
</html>